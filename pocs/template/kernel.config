# Optional per-PoC kernel config fragment.
# Add only the options required by the copied PoC, then keep this path in
# poc.yaml. If the copied PoC needs no extra kernel options, remove the
# kernel_config line from poc.yaml.
